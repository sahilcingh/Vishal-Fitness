NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Real production domain (used by sitemap.xml, robots.txt, Open Graph tags,
# and JSON-LD). Falls back to a placeholder if unset — must be set before
# going live or all of the above will point at the wrong domain.
NEXT_PUBLIC_SITE_URL=
